# rustdesk二次开发指引

## 与上游的同步策略

二开项目的长期可维护性取决于能否跟随上游升级。本项目采用以下策略：

1. **改动局部化**：对上游代码的改动应尽量最小化并集中在少数文件。优先以"新增独立模块/文件"的方式实现定制功能（新模块挂入现有模块树即可）；必须修改上游已有文件时，只做最小的必要改动，并在改动处用 `// dec:` 注释标注，便于升级时快速定位。

2. **改动点登记**：维护一份 [`docs/upstream-patches.md`](upstream-patches.md)，记录每个对上游文件的修改位置、目的、与上游版本的兼容性。每次升级前先复核此清单。

3. **升级流程**：
   - 拉取上游最新 tag，在一个干净分支上合并
   - 跑全量测试与静态检查，重点验证 `tests/integration_tests.rs` 等集成测试以及本次改动涉及的模块
   - 若合并冲突或测试失败，按 `docs/upstream-patches.md` 逐点核对本地改动与上游变更，手动解决；**绝不**直接 `git checkout` 上游文件覆盖本地改动。

4. **依赖收敛**：
   - 工作区依赖以根目录 `Cargo.toml` 的 `[workspace.dependencies]` 为单一版本来源，各 crate 通过 `<dep>.workspace = true` 引用，升级依赖只需改一处。
   - 升级上游时优先采纳上游 `Cargo.lock` 中已解析的版本，避免本地依赖与上游漂移造成无谓冲突。
   - 新增依赖需谨慎：优先使用标准库或上游已引入的依赖；确需新增时，理由写入 PR 说明。

5. **不引入破坏性删除**：不删除、不改名上游公开的 API、字段或 feature，以免上游升级时配置层和调用层冲突。上游 API 不再满足需求时，优先在其上层做适配或新增扩展点；确实需要移除时，先在 issue 中说明并给出迁移路径。
